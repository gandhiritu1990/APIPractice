package com.basics;

public class JiraBasic {

    public static void main(String[] args){
        //SessionFilter class is used to store session and use it befor when method uaing .filter(session)
        //just create an object of SessionFilter
        //multiPart method is used to add attachment which takes file path as an argument .multiPart("file",new File("filePath"))
        //given().relaxedHTTPSValidation() to restrict SSL certification
    }
}
